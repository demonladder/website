import Container from '../../../../components/Container';

export default function Settings() {
    return (
        <Container>
            <h1 className='text-4xl'>Profile settings</h1>
            <p>There's nothing here yet, go somewhere else {'>:('}</p>
        </Container>
    );
}