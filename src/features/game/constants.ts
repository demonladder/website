import Player from './gameObjects/Player';

export const pixelsPerBlock = 60;
export const camOffset = 8; // Blocks
export const keysPressed: Record<string, boolean> = {};
export const player = new Player();
